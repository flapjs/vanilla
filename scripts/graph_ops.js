/** @module graph_ops */

function is_edge_valid(graph, edge) {

}

export function is_NFA() {
  return document.getElementById('select_machine').value === 'NFA';
}

export function is_Pushdown() {
  return document.getElementById('select_machine').value === 'Pushdown';
}
